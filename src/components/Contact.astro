<section id="contact" class="contact-section">
  <div class="contact-container">

    <div class="contact-intro">
      <p class="eyebrow">Let's connect</p>

      <h2>
        Have an idea?
        <br />
        Let's make it real.
      </h2>

      <p class="description">
        Whether you have a project in mind, need help building an application,
        or simply want to talk about technology, feel free to reach out.
      </p>
    </div>

    <div class="contact-content">

      <a
        href="mailto:asieldi04@email.com"
        class="contact-card"
      >
        <span class="icon">✉</span>

        <div>
          <span class="label">Email</span>
          <span class="value">asieldi04@email.com</span>
        </div>

        <span class="arrow">↗</span>
      </a>

      <a
        href="https://github.com/zoyaa26"
        target="_blank"
        rel="noopener noreferrer"
        class="contact-card"
      >
        <span class="icon">⌘</span>

        <div>
          <span class="label">GitHub</span>
          <span class="value">zoyaa26</span>
        </div>

        <span class="arrow">↗</span>
      </a>

      <a
        href="https://www.linkedin.com/in/eka-herlambang-852070311/"
        target="_blank"
        rel="noopener noreferrer"
        class="contact-card"
      >
        <span class="icon">in</span>

        <div>
          <span class="label">LinkedIn</span>
          <span class="value">ekaherlambang</span>
        </div>

        <span class="arrow">↗</span>
      </a>

    </div>

  </div>

  <div class="contact-footer">
    <span>© {new Date().getFullYear()} Eka Herlambang</span>
  </div>
</section>

<style>
  .contact-section {
    position: relative;
    padding: 120px 7vw 35px;
    background: var(--background);
    color: var(--text);
    border-top: 1px solid var(--border);
    overflow: hidden;
  }

  .contact-container {
    max-width: 1150px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .eyebrow {
    margin: 0 0 16px;
    color: var(--primary);
    font-family: Georgia, serif;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  .contact-intro h2 {
    margin: 0;
    font-family: Georgia, serif;
    font-size: clamp(42px, 5vw, 68px);
    line-height: 1.05;
    font-weight: 500;
    letter-spacing: -0.03em;
  }

  .description {
    max-width: 520px;
    margin: 28px 0 0;
    color: var(--text-soft);
    font-family: Georgia, serif;
    font-size: 17px;
    line-height: 1.8;
  }

  .contact-content {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .contact-card {
    display: flex;
    align-items: center;
    gap: 18px;
    padding: 20px 22px;
    border: 1px solid var(--border);
    border-radius: 16px;
    background: var(--surface);
    color: var(--text);
    text-decoration: none;
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      border-color 0.25s ease;
  }

  .contact-card:hover {
    transform: translateY(-4px);
    border-color: rgba(138, 90, 68, 0.35);
    box-shadow: 0 12px 30px rgba(60, 40, 30, 0.1);
  }

  .icon {
    width: 44px;
    height: 44px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    background: #faf3dc;
    color: #8a5a44;
    font-family: Arial, sans-serif;
    font-size: 17px;
    font-weight: 700;
  }

  .contact-card > div {
    display: flex;
    flex-direction: column;
    gap: 4px;
    min-width: 0;
  }

  .label {
    color: var(--text-soft);
    font-family: Arial, sans-serif;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .value {
    overflow: hidden;
    color: var(--text);
    font-family: Georgia, serif;
    font-size: 15px;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .arrow {
    margin-left: auto;
    color: var(--primary);
    font-size: 20px;
    transition: transform 0.25s ease;
  }

  .contact-card:hover .arrow {
    transform: translate(3px, -3px);
  }

  .contact-footer {
    max-width: 1150px;
    margin: 100px auto 0;
    padding-top: 22px;
    display: flex;
    justify-content: space-between;
    border-top: 1px solid var(--border);
    color: var(--text-soft);
    font-family: Arial, sans-serif;
    font-size: 11px;
    letter-spacing: 0.04em;
  }

  /* =========================
     FORMAL MODE
  ========================= */

  html[data-theme="formal"] .contact-section {
    background: #ffffff;
    color: #171717;
    border-color: #e5e5e5;
  }

  html[data-theme="formal"] .eyebrow {
    color: #555555;
    font-family: Arial, sans-serif;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  html[data-theme="formal"] .contact-intro h2 {
    font-family: Arial, sans-serif;
    font-weight: 700;
    letter-spacing: -0.045em;
  }

  html[data-theme="formal"] .description {
    color: #666666;
    font-family: Arial, sans-serif;
  }

  html[data-theme="formal"] .contact-card {
    background: #ffffff;
    border-color: #dedede;
  }

  html[data-theme="formal"] .contact-card:hover {
    border-color: #171717;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.06);
  }

  html[data-theme="formal"] .icon {
    background: #f3f3f3;
    color: #171717;
  }

  html[data-theme="formal"] .label {
    color: #777777;
  }

  html[data-theme="formal"] .value {
    color: #171717;
    font-family: Arial, sans-serif;
  }

  html[data-theme="formal"] .arrow {
    color: #171717;
  }

  html[data-theme="formal"] .contact-footer {
    color: #777777;
    border-color: #e5e5e5;
  }

  /* =========================
     RESPONSIVE
  ========================= */

  @media (max-width: 900px) {
    .contact-container {
      grid-template-columns: 1fr;
      gap: 50px;
    }

    .contact-intro h2 {
      font-size: clamp(40px, 8vw, 58px);
    }

    .contact-footer {
      margin-top: 80px;
    }
  }

  @media (max-width: 600px) {
    .contact-section {
      padding: 90px 6vw 28px;
    }

    .contact-intro h2 {
      font-size: 42px;
    }

    .description {
      font-size: 15px;
      line-height: 1.7;
    }

    .contact-card {
      padding: 17px;
      border-radius: 14px;
    }

    .contact-footer {
      margin-top: 60px;
      flex-direction: column;
      gap: 8px;
    }
  }

  @media (max-width: 380px) {
    .contact-intro h2 {
      font-size: 36px;
    }

    .contact-card {
      gap: 12px;
    }

    .icon {
      width: 40px;
      height: 40px;
    }

    .value {
      font-size: 13px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .contact-card,
    .arrow {
      transition: none;
    }
  }
</style>