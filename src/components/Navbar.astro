<a href="#top" class="logo" aria-label="Back to top">Eka.</a>

<nav class="side-nav" aria-label="Main navigation">
  <div class="nav-pill">

    <!-- Home -->
    <a href="#top" class="nav-item active" aria-label="Home">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M3 10.5 12 3l9 7.5" />
        <path d="M5.5 9.5V21h13V9.5" />
        <path d="M9.5 21v-6h5v6" />
      </svg>
    </a>

    <!-- About -->
    <a href="#about" class="nav-item" aria-label="About">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <circle cx="12" cy="8" r="3.5" />
        <path d="M5 21c.8-4 3.2-6 7-6s6.2 2 7 6" />
      </svg>
    </a>

    <!-- Skills -->
    <a href="#skills" class="nav-item" aria-label="Skills">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M12 3 14.2 9.8 21 12l-6.8 2.2L12 21l-2.2-6.8L3 12l6.8-2.2L12 3Z" />
      </svg>
    </a>

    <!-- Services -->
    <a href="#services" class="nav-item" aria-label="Services">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <rect x="3" y="7" width="18" height="13" rx="2" />
        <path d="M8 7V5.5A2.5 2.5 0 0 1 10.5 3h3A2.5 2.5 0 0 1 16 5.5V7" />
        <path d="M3 12h18" />
        <path d="M10 12v2h4v-2" />
      </svg>
    </a>

    <!-- Projects -->
    <a href="#projects" class="nav-item" aria-label="Projects">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <rect x="3" y="4" width="18" height="16" rx="2" />
        <path d="M7 8h10" />
        <path d="M7 12h6" />
        <path d="M7 16h8" />
      </svg>
    </a>

    <div class="nav-divider"></div>

    <!-- Theme -->
    <button
      type="button"
      class="theme-toggle"
      aria-label="Switch theme"
      title="Switch theme"
    >
      <svg class="theme-icon ghibli-icon" viewBox="0 0 24 24" aria-hidden="true">
        <circle cx="12" cy="12" r="4" />
        <path d="M12 2v2" />
        <path d="M12 20v2" />
        <path d="m4.93 4.93 1.41 1.41" />
        <path d="m17.66 17.66 1.41 1.41" />
        <path d="M2 12h2" />
        <path d="M20 12h2" />
        <path d="m4.93 19.07 1.41-1.41" />
        <path d="m17.66 6.34 1.41-1.41" />
      </svg>

      <svg class="theme-icon formal-icon" viewBox="0 0 24 24" aria-hidden="true">
        <path d="M12 3v18" />
        <path d="M5 7h14" />
        <path d="M7 7 4 14h6L7 7Z" />
        <path d="M17 7 14 14h6l-3-7Z" />
        <path d="M8 21h8" />
      </svg>
    </button>

  </div>
</nav>

<script>
  const themeButton = document.querySelector(".theme-toggle");

  const savedTheme = localStorage.getItem("portfolio-theme");

  if (savedTheme === "formal" || savedTheme === "ghibli") {
    document.documentElement.dataset.theme = savedTheme;
  } else {
    document.documentElement.dataset.theme = "ghibli";
  }

  themeButton?.addEventListener("click", () => {
    const currentTheme =
      document.documentElement.dataset.theme || "ghibli";

    const nextTheme =
      currentTheme === "ghibli" ? "formal" : "ghibli";

    document.documentElement.dataset.theme = nextTheme;
    localStorage.setItem("portfolio-theme", nextTheme);
  });
</script>

<style>
  .logo {
    position: fixed;
    top: 28px;
    left: 5vw;
    z-index: 1100;

    color: var(--text);
    font-family: Georgia, serif;
    font-size: 27px;
    font-weight: 700;
    line-height: 1;
    text-decoration: none;

    transition: color 0.45s ease;
  }

  .side-nav {
    position: fixed;
    left: 28px;
    top: 50%;
    z-index: 1000;

    transform: translateY(-50%);
  }

  .nav-pill {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;

    width: 62px;
    padding: 10px 7px;

    background: rgba(255, 255, 255, 0.72);
    border: 1px solid var(--border);
    border-radius: 32px;

    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);

    box-shadow: 0 12px 35px rgba(0, 0, 0, 0.08);

    transition:
      background 0.45s ease,
      border-color 0.45s ease,
      box-shadow 0.45s ease;
  }

  .nav-item,
  .theme-toggle {
    width: 46px;
    height: 46px;

    display: flex;
    align-items: center;
    justify-content: center;

    border: 0;
    border-radius: 50%;

    color: var(--text-soft);
    background: transparent;

    text-decoration: none;
    cursor: pointer;

    transition:
      background 0.3s ease,
      color 0.3s ease,
      transform 0.3s ease;
  }

  .nav-item:hover,
  .theme-toggle:hover {
    color: var(--text);
    background: rgba(127, 127, 127, 0.1);
    transform: scale(1.05);
  }

  .nav-item.active {
    color: var(--text);
    background: rgba(127, 127, 127, 0.12);
  }

  .nav-item svg,
  .theme-toggle svg {
    width: 19px;
    height: 19px;

    fill: none;
    stroke: currentColor;
    stroke-width: 1.7;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  /* Bagian Divider diubah menjadi tanpa garis (hanya memberi jarak spasi) */
  .nav-divider {
    width: 100%;
    height: 12px;
    margin: 4px 0;
    background: transparent;
    border: none;
  }

  .formal-icon {
    display: none;
  }

  html[data-theme="formal"] .ghibli-icon {
    display: none;
  }

  html[data-theme="formal"] .formal-icon {
    display: block;
  }

  html[data-theme="formal"] .nav-pill {
    background: rgba(255, 255, 255, 0.82);
  }

  @media (max-width: 900px) {
    .logo {
      top: 22px;
      left: 4vw;
      font-size: 24px;
    }

    .side-nav {
      left: 18px;
    }

    .nav-pill {
      width: 58px;
    }

    .nav-item,
    .theme-toggle {
      width: 44px;
      height: 44px;
    }
  }

  @media (max-width: 600px) {
    .logo {
      top: 18px;
      left: 20px;
      font-size: 23px;
    }

    .side-nav {
      left: 50%;
      top: auto;
      bottom: 15px;
      transform: translateX(-50%);
    }

    .nav-pill {
      flex-direction: row;
      width: auto;
      padding: 7px 9px;
      gap: 3px;
    }

    /* Penyesuaian jarak kosong untuk mode mobile (horizontal) */
    .nav-divider {
      width: 12px;
      height: 100%;
      margin: 0 4px;
    }

    .nav-item,
    .theme-toggle {
      width: 42px;
      height: 42px;
    }
  }
</style>